apiVersion: skaffold/v2alpha3
kind: Congig
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  # local:
  #   push: false
  googleCloudBuild:
    projectId: ticketing-dev-300517
  artifacts:
    - image: us.gcr.io/ticketing-dev-300517/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .